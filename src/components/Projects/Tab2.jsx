import React from 'react';
import Table from '../table/Table';
import MockContent from '../mock-content';

const Tab2 = () => {
  return (
    <section style={{ backgroundColor: 'white', padding: '2em' }} className="Closed / Refuted Reports">
      <MockContent name="Closed / Refuted Reports" />
    </section>
  );
};

export default Tab2;
